// This file exports the Postiner logo SVG for use in components.
import React from "react";

const PostinerLogo = (props: React.SVGProps<SVGSVGElement>) => (
  <svg {...props}>
    {/* SVG content will be inlined by import */}
  </svg>
);

export default PostinerLogo;
